Sync Flow 设计 (v2)
======================

注: 本文描述当前同步 flow。

核心设计原则：
  1. 所有网络IO并发执行 (http_batch为唯一出口)
  2. 阶段化流程，每个阶段内部并发，阶段之间有依赖
  3. 多行进度显示 (参考rebuild.py ProgressBoard)
  4. 异常记录到log文件，终端只显示进度

======================
进度显示设计 (ProgressBoard)
======================

采用多行ANSI转义码，每个阶段一行，实时更新：

  [aggregate] resolve snapshot block: 2/2 subgraphs
  [aggregate] fetch user positions: 50/100 users
  [aggregate] fetch market data: 0/0 tokens
  [aggregate] fetch conditions: 0/0 conditions
  [aggregate] fetch gamma meta: 0/0 markets

实现要点：
  - 初始化时打印所有stage行
  - 更新时用 \x1b[{lines_up}A 上移光标，\x1b[K 清行
  - 用mutex保护打印

======================
full_resync() 新设计
======================

Stage 1: resolve_snapshot_block (并发2个subgraph meta查询)
  http_batch([
    POST thegraph/pnl/_meta,
    POST thegraph/poly/_meta,
  ])
  -> snapshot_block = min(pnl, poly) - 64
  注: snapshot_block 不需要等于 head，只需要是稳定且固定的 block

Stage 2: fetch_user_positions (并发N个用户组)
  用户分组: chunked(users, 50)
  每组独立分页查询 (单组内串行分页，组间并发)
  http_batch([
    POST userPositions(group1, page1),
    POST userPositions(group2, page1),
    ...
  ])
  注: 分页需要串行(依赖上一页cursor)，但不同组可以并发

Stage 3: fetch_stable_balances (并发RPC)
  http_batch([
    POST rpc balanceOf(user1, USDC.e),
    POST rpc balanceOf(user1, wrapped),
    POST rpc balanceOf(user2, USDC.e),
    ...
  ])
  或者用eth_call batch打包

Stage 4: fetch_market_data (并发多个token批次)
  token_ids = 从positions收集
  http_batch([
    POST thegraph/poly/marketDatas(batch1),
    POST thegraph/poly/marketDatas(batch2),
    ...
  ])

Stage 5: fetch_conditions (并发多个condition批次)
  condition_ids = 从market_data收集
  http_batch([
    POST thegraph/pnl/conditions(batch1),
    POST thegraph/pnl/conditions(batch2),
    ...
  ])

Stage 6: fetch_gamma_meta (并发gamma API)
  condition_ids = 缺少market_question的
  http_batch([
    GET gamma/markets?condition_ids=xxx,
    ...
  ])

Stage 7: start_ws_subscription
  建立 websocket 订阅:
    eth_subscribe newHeads 作为 block flush 信号
    eth_subscribe logs 接收实时日志
  注: 先建订阅，再补 gap，避免订阅建立窗口漏事件

Stage 8: backfill_gap (RPC 并发多个filter)
  用 rpc_block_number / eth_getLogs 补齐 snapshot_block 之后到订阅建立时刻之间的 gap
  注: backfill_gap 和 websocket logs 使用统一 raw log 格式，落地到 history 时不区分来源
  filters = build_log_filters(users, from_block, to_block)
  http_batch([
    POST rpc eth_getLogs(filter1),
    POST rpc eth_getLogs(filter2),
    ...
  ])

Stage 9: persist_all

======================
http_batch 改进
======================

当前http_batch:
  - 接收 vector<HttpRequest>
  - 并发执行，无限重试
  - 返回 vector<HttpResponse>

改进:
  1. 支持回调函数处理单个响应 (可选)
  2. 内部错误记录到log文件
  3. 限制单个请求最大重试时间 (如30s)

======================
Log 设计
======================

文件: data/tracker/sync.log (按日期轮转)

记录内容:
  - 网络请求失败/重试
  - subgraph "bad indexers" 错误
  - JSON解析失败
  - 其他异常

格式:
  2024-01-15T12:34:56 [WARN] subgraph retry: bad indexers, attempt=3
  2024-01-15T12:34:57 [ERROR] http timeout: gamma-api.polymarket.com

终端只显示ProgressBoard，不输出debug信息。

======================
依赖关系
======================

  resolve_snapshot_block
          |
          v
  fetch_user_positions
          |
          v
  fetch_stable_balances (可与下一步并行)
          |
          v
  fetch_market_data
          |
          v
  fetch_conditions
          |
          v
  fetch_gamma_meta
          |
          v
  start_ws_subscription
          |
          v
  backfill_gap
          |
          v
  persist_all

增量阶段:
  websocket logs / newHeads
          |
          v
  apply_logs / 更新运行时状态
          |
          v
  aggregate 重算
          |
          v
  persist_all

======================
数据存储设计
======================

目录结构:
  data/tracker/
    snapshot.json   - 用户持仓快照 (历史记录)
    history.json    - 交易记录 (历史记录)
    aggregate.json  - 聚合仓位 (实时状态，每次sync/交易更新)
    meta.json       - token/condition元数据
    sync.log        - 同步日志

--- snapshot.json ---
存储用户在特定区块的详细持仓情况 (历史快照)

{
  "0xuser1": {
    "12345678": {                    // block_num as key
      "block_number": 12345678,
      "captured_at_unix_sec": 1705312496,
      "stable_balances": {
        "usdc_e_raw": "1000000000",
        "wrapped_collateral_raw": "500000000"
      },
      "positions": [
        {"token_id": "123...", "amount_raw": "1000000"}
      ]
    }
  }
}

--- history.json ---
存储统一格式的历史交易记录
来源可能是 websocket 实时日志，也可能是启动/重连时通过 RPC 补齐的 gap 日志；落地时不做来源区分

{
  "0xuser1": {
    "12345678": [                    // block_num as key, array of events
      {
        "tx_hash": "0x...",
        "log_index": 5,
        "direction": "in",           // "in" | "out"
        "kind": "order_buy",         // order_buy, order_sell, split_in, merge_out, ...
        "token_id": "123...",
        "amount_raw": "1000000",
        "counterparty": "0x...",
        "price": "0.5500000000",     // if order fill
        "collateral_amount_raw": "550000"
      }
    ]
  }
}

--- aggregate.json ---
聚合仓位 (实时计算的当前状态)
- 每次 snapshot / gap 补齐完成后更新
- 每次 websocket 增量事件应用后触发重算
- 用于未来交易决策

{
  "summary": {
    "snapshot_block": 12345678,
    "last_applied_block": 12345700,
    "user_count": 10,
    "position_count": 150
  },
  "users": [...],
  "aggregate": [
    {
      "token_id": "123...",
      "holder_count": 5,
      "aggregate_weight": "0.1500000000"
    }
  ]
}

--- meta.json ---
token和condition元数据 (与之前相同)

{
  "tokens": { ... },
  "conditions": { ... }
}

======================
UI 设计

A:用户列表(token类资产市值/总市值(加上类USDC稳定币资产)):
B:交易记录(按照block number, 展示 snapshot 和统一格式的历史交易记录，不区分 websocket / RPC 来源)
C:选择栏: 可以选择 aggre当前动态持仓, 用户当前动态持仓, 用户历史静态持仓(选择历史记录里的snapshot)
D:持仓展示: 每个token按百分比展示bar, 显示yes/no/index, 持仓比例(USD计价), 里面包括稳定币资产, 鼠标悬浮展示问题和描述
E:query count (展示rpc,subgraph当前session分别query了多少次)

----------------------------
|                |         |
|                |         |
|       A        |    B    |
|                |         |
----------------------------
| C                      E |
|                          |
|                          |
|                          |
|            D             |
|                          |
|                          |
|                          |
----------------------------