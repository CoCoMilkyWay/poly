======================
线程架构
======================
                                  ┌─────────────┐
                                  │ 前端 UI     │
                                  │ (浏览器)    │
                                  └──────┬──────┘
                                         │ HTTP API
                                         v
┌─────────────────────────────────────────────────────────────────────────────┐
│                                  后端进程                                   │
│                                                                             │
│  ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐        │
│  │    WS Thread      │  │   Sync Thread     │  │   HTTP Thread     │        │
│  │  (RPC websocket)  │  │   (状态持有者)    │  │  (前端API服务)    │        │
│  ├───────────────────┤  ├───────────────────┤  ├───────────────────┤        │
│  │ 输入: RPC节点     │  │ 核心业务线程      │  │ 输入: 前端请求    │        │
│  │ 输出: event_queue │  │                   │  │ 输出: JSON响应    │        │
│  │                   │  │                   │  │                   │        │
│  │ - 维护 ws 连接    │  │ - 拥有全部状态    │  │ - /api/state      │        │
│  │ - eth_subscribe   │  │ - full_resync()   │  │ - /api/meta       │        │
│  │   newHeads/logs   │  │ - drain 队列      │  │ - /api/history    │        │
│  │ - 解析后入队列    │  │ - apply_logs()    │  │ - /api/resync     │        │
│  │ - 断线自动重连    │  │ - persist_all()   │  │                   │        │
│  │                   │  │ - 发布 state_ptr  │  │ 读 state_ptr 响应 │        │
│  └─────────┬─────────┘  └─────────┬─────────┘  └─────────┬─────────┘        │
│            │                      │                      │                  │
│            │    event_queue       │      state_ptr       │                  │
│            │    (mpsc, 写)        │   (atomic, 只读)     │                  │
│            └─────────────────────>│<─────────────────────┘                  │
│                                   │                                         │
│                                   │  resync_flag (atomic<bool>)             │
│                                   │<────────────────────────────────────────│
│                                   │                                         │
│                                   v                                         │
│                          ┌─────────────────┐                                │
│                          │   http_batch()  │                                │
│                          │   无状态纯函数  │                                │
│                          │   阻塞调用      │                                │
│                          │   内部asio并发  │                                │
│                          └─────────────────┘                                │
│                                   │                                         │
│                                   v                                         │
│                    ┌──────────────────────────────┐                         │
│                    │ RPC / TheGraph / Gamma API   │                         │
│                    └──────────────────────────────┘                         │
└─────────────────────────────────────────────────────────────────────────────┘

api.hpp      # API Thread 接口
codec.hpp    # 编码解码工具
config.hpp   # 配置
const.hpp    # 合约地址、Topic 常量
http.hpp     # HTTP 客户端
json.hpp     # json alias
log.hpp      # 日志 + 进度显示
queue.hpp    # event_queue (mpsc)
state.hpp    # 状态结构定义
store.hpp    # JSON 存储
sync.hpp     # Sync Thread 接口
ws.hpp       # WS Thread 接口
main.cpp     # 入口，启动3线程

======================
范围约束
======================
  - 仅支持 Polymarket 当前 binary 市场
  - 仅支持 parent_collection_id = 0x0 的直接仓位
  - 不支持一般化 CTF collection / 多层 collection

======================
前端UI设计
======================
A:用户列表(token类资产市值/总市值(加上类USDC稳定币资产)):
B:交易记录(按照block number, 展示 snapshot 和统一格式的历史交易记录,不区分 websocket / RPC 来源)
C:选择栏: 可以选择 aggre当前动态持仓, 用户当前动态持仓, 用户历史静态持仓(选择历史记录里的snapshot)
D:持仓展示: 每个token按百分比展示bar, 显示yes/no/index, 持仓比例(USD计价), 里面包括稳定币资产, 鼠标悬浮展示问题和描述
E:query count (展示rpc,subgraph当前session分别query了多少次)

----------------------------
|                |         |
|       A        |    B    |
|                |         |
----------------------------
| C                      E |
|                          |
|            D             |
|                          |
----------------------------

API:
a/{users}      Graph(pnl).userPositions(1)                           获取用户历史snapshot持仓({kGraphPageLimit}分页)
b/4            RPC.eth_call({users})                                 批量获取用户稳定币余额 (USDC/USDCe/USDT/Wrapped)
c/{tokens}     Gamma.markets({kGammaBatchLimit})                     批量获取token/condition元数据
d/             ws.RPC.eth_subscribe({users})                         批量订阅WebSocket交易记录(subscribe={subscribe},usrs/subscribe/topic={kTopicGroupSize})
e/             RPC.eth_blockNumber                                   获取当前区块高度
f/             RPC.eth_getLogs({users})                              批量补齐历史记录({kGetLogsBlockSpan}区块拆分)

======================
进度显示设计(终端)
======================
  - 按 full_resync 流程顺序显示
  - 底部显示当前 stage
  - 更新时用 \x1b[{lines_up}A 上移光标,\x1b[K 清行
  - 用 mutex 保护打印

终端显示格式:
  API       done/total  [pend]   说明
  snapshot  5/10        [3]      [a] done=完成用户数, total=用户数
  stables   40/40       [0]      [b] done=完成查询数, total=用户数*4
  meta      50/100      [5]      [c] done=完成token数, total=token数
  ws_sub    10/10       [0]      [d] done=订阅用户数, total=用户数
  head      1/1         [0]      [e] done=1表示完成
  backfill  500/1000    [10]     [f] done=已处理区块数, total=区块范围
  [current_stage]
======================
Log设计(文件)
======================
  - 网络请求失败/无限重试(预期此程序7*24小时运行)
  - Graph/Gamma API 错误
  - JSON解析失败
  - 其他异常
======================
数据存储设计
======================
目录结构:
  data/
    snapshot.json   - 用户持仓快照 (历史记录)
    history.json    - 交易记录 (历史记录)
    aggregate.json  - 聚合仓位 (实时状态,每次sync/交易更新)
    meta.json       - token/condition元数据
    sync.log        - 同步日志
======================
整体流程
======================
```
文件落地时机:
  S = snapshot.json (用户持仓快照)
  H = history.json (交易记录)
  A = aggregate.json (聚合仓位)
  M = meta.json (token/condition元数据)

SyncThread.run()
├─ 初始化
│  ├─ load_seed() → tokens/conditions 初始映射
│  └─ load_files() → snapshot_root/history_root 从磁盘恢复
├─ 主循环
│  ├─ full_resync() (初始触发/每3600s定时触发/手动UI触发)
│  │  ├─ [a] fetch_user_snapshots (per-user 并发)
│  │  │  ├─ API: Graph(pnl).userPositions(user)
│  │  │  ├─ 输出: user_snapshots[user] = {snapshot_block, positions[]}
│  │  │  ├─ 注: snapshot_block 是 per-user checkpoint, 不是全局统一块高
│  │  │  └─ 落地: → S (snapshot.json)
│  │  ├─ [b] fetch_snapshot_balances (并发 RPC)
│  │  │  ├─ API: RPC.eth_call balanceOf(user, USDC/USDCe/USDT/Wrapped)
│  │  │  ├─ 输出: user_states[user].stable = {usdc, usdc_e, usdt, wrapped}
│  │  │  └─ 注: 只在 full_resync 时更新，不监听 ERC20 Transfer
│  │  ├─ [c] fetch_gamma_by_token_ids (批量)
│  │  │  ├─ API: Gamma.markets?clob_token_ids=tid1,tid2,...
│  │  │  ├─ 输出: tokens[token_id] = {condition_id, idx}
│  │  │  │        conditions[cond_id] = {oc, coll, tids[], q, slug, outcomes[]}
│  │  │  ├─ 注: Gamma 一步返回完整 token/condition 元数据
│  │  │  └─ 落地: → M (meta.json)
│  │  ├─ [d] start_ws_subscription (先订阅，再获取head，再backfill)
│  │  │  ├─ API: ws.RPC.eth_subscribe(newHeads, logs)
│  │  │  ├─ 阻塞等待: ws 连接成功 + 收到首条 newHeads
│  │  │  ├─ 输出: ws 连接建立, ws_start_block = 首条 newHeads.number
│  │  │  └─ 注: ws 事件暂存队列，等 backfill 完成后再处理
│  │  ├─ [e] get_head_block (订阅成功后获取)
│  │  │  ├─ API: RPC.eth_blockNumber()
│  │  │  └─ 输出: head_block = max(ws_start_block, eth_blockNumber)
│  │  ├─ [f] backfill_range (按 per-user checkpoint 计算, RPC 侧合并抓取)
│  │  │  ├─ 对每个 user: from = user_snapshots[user].snapshot_block + 1, to = head_block
│  │  │  ├─ API: RPC.eth_getLogs({users}, merged filters)
│  │  │  ├─ 处理: 先按 raw_log_key 合并去重, 再 apply_block_logs(logs)
│  │  │  ├─ 落地: → H (history.json) per batch, 按 event_id 幂等写入
│  │  │  └─ 完成后: backfill_done_block = head_block
│  │  ├─ 去重处理 (多 filter / 多 user / backfill 与 ws 重叠)
│  │  │  ├─ 遍历 ws 暂存队列中 block <= backfill_done_block 的事件
│  │  │  ├─ raw 去重: 相同 raw_log_key 只保留一份 raw log
│  │  │  ├─ emit 后: event_id = op_key + "|" + user + "|" + leg_index
│  │  │  └─ H 落地按 event_id 幂等
│  │  └─ 计算 aggregate
│  │     ├─ 输出: aggregate 状态 + summary.min_snapshot_block
│  │     ├─ 输出: summary.snapshot_blocks[user] = user_snapshots[user].snapshot_block
│  │     └─ 落地: → A (aggregate.json)
│  ├─ drain_queue() (处理 websocket 事件)
│  │  ├─ on newHeads → update head_block
│  │  └─ on logs
│  │     ├─ apply_block_logs([log])
│  │     ├─ 落地: → H (history.json) 增量, 按 event_id 幂等写入
│  │     ├─ 映射补齐时: → M (meta.json) via Gamma
│  │     └─ aggregate 重算: → A (aggregate.json)
│  └─ sleep(100ms)  // 主循环空闲轮询间隔
└─ 落地汇总
   ├─ S (snapshot.json): [a] 完成后
   ├─ M (meta.json): [c] 完成后 + 增量映射补齐时 (via Gamma)
   ├─ H (history.json): [f] 每批 + ws 增量, 按 event_id 幂等
   └─ A (aggregate.json): [f] 完成后 + ws 每条事件后
```

======================
apply_block_logs(logs) 流程
======================
```
apply_block_logs(logs)
├─ 输入
│  ├─ logs[]: raw RPC logs (按 block + tx + log_index 排序)
│  └─ state: tokens/conditions 映射 (可被补齐更新)
├─ 前置约束
│  ├─ assert(parent_collection_id == 0x0)
│  └─ assert(outcome_count == 2)
├─ 主循环 for log in logs
│  ├─ OrderFilled (CTF_EXCHANGE | NEG_RISK_CTF_EXCHANGE)
│  │  ├─ 解码: maker, taker, maker_asset_id, taker_asset_id, maker_amt, taker_amt, fee
│  │  ├─ 判断: maker_is_buyer = (maker_asset_id == 0)
│  │  │  ├─ buyer = maker_is_buyer ? maker : taker
│  │  │  ├─ seller = maker_is_buyer ? taker : maker
│  │  │  ├─ token_id = maker_is_buyer ? taker_asset_id : maker_asset_id
│  │  │  ├─ token_amount = maker_is_buyer ? taker_amt : maker_amt
│  │  │  └─ collateral_amount = maker_is_buyer ? maker_amt : taker_amt
│  │  ├─ 补齐: token_id → (condition_id, token_idx) from tokens[]
│  │  ├─ 产出 (双方各一条):
│  │  │  ├─ emit(buyer, OrderBuy, +token_amount, price=coll*1e6/tokens)
│  │  │  └─ emit(seller, OrderSell, -token_amount, price=coll*1e6/tokens)
│  │  └─ 更新: user_states[buyer].positions[token_id] += token_amount
│  │          user_states[seller].positions[token_id] -= token_amount
│  ├─ PositionSplit (CONDITIONAL_TOKENS)
│  │  ├─ 解码: stakeholder, condition_id, partition[], amount, collateral_token
│  │  ├─ 补齐: condition_id → (outcome_count, collateral) from conditions[]
│  │  │  └─ 缺失时: on-demand query Gamma API
│  │  ├─ 计算: token_ids[] = [getPositionId(coll, cond, idx) for idx in partition]
│  │  ├─ 产出 (每个 outcome 一条):
│  │  │  └─ emit(stakeholder, Split, +amount, price=1e6/outcome_count)
│  │  └─ 更新: user_states[stakeholder].positions[token_id] += amount (per token)
│  ├─ PositionsMerge (CONDITIONAL_TOKENS)
│  │  ├─ 解码: stakeholder, condition_id, partition[], amount, collateral_token
│  │  ├─ 补齐/计算: 同 Split
│  │  ├─ 产出 (每个 outcome 一条):
│  │  │  └─ emit(stakeholder, Merge, -amount, price=1e6/outcome_count)
│  │  └─ 更新: user_states[stakeholder].positions[token_id] -= amount (per token)
│  ├─ PayoutRedemption (CONDITIONAL_TOKENS)
│  │  ├─ 解码: redeemer, condition_id, index_sets[], payout
│  │  ├─ 补齐: condition_id → (payout_numerators[], payout_denominator) from conditions[]
│  │  ├─ 计算 holding (事件只有 payout 总额，需反推):
│  │  │  ├─ winner_idx = argmax(payout_numerators)
│  │  │  ├─ winner_ratio = numerators[winner_idx] / payout_denominator
│  │  │  ├─ holding = payout / winner_ratio  (赎回前持有量)
│  │  │  └─ 注: 只有获胜方有 payout，其他 index_set 的 holding 从当前持仓查
│  │  ├─ 产出 (每个 index_set 一条):
│  │  │  ├─ price = numerators[idx] * 1e6 / payout_denominator
│  │  │  └─ emit(redeemer, Redeem, -holding[idx], price)
│  │  └─ 更新: user_states[redeemer].positions[token_id] = 0 (清仓)
│  └─ PositionConvert (NEG_RISK_ADAPTER; 只作为 convert op root)
│     ├─ 解码: stakeholder, market_id, index_set, amount
│     ├─ 补齐: market_id → question_ids[] → condition_ids[]
│     │  └─ 缺失时: Gamma /markets?question_ids= + /events?slug=
│     ├─ 不直接 emit 单条 Convert
│     ├─ 同 tx 查找相关 TransferSingle/TransferBatch 仓位腿
│     │  ├─ burn 腿: stakeholder/adapter -> burn_address 的旧 token
│     │  ├─ mint/transfer 腿: 0x0/adapter -> stakeholder 或 vault 的新 token
│     │  └─ 每条腿映射到 (condition_id, token_idx, signed_amount)
│     ├─ 以 PositionConvert raw log 作为 op_key, 对展开后的每条仓位腿分配 leg_index
│     ├─ 产出: emit(stakeholder, Convert, signed_amount, price=0)
│     └─ 更新: user_states[stakeholder].positions += signed_amount (per token)
├─ 映射补齐 (on-demand, 遇到未知映射时触发, 统一用 Gamma)
│  ├─ token_id 未知 → Gamma /markets?clob_token_ids=
│  │  └─ 返回: conditionId, clobTokenIds[], question, outcomes
│  │  └─ 从 clobTokenIds 数组位置推断 idx
│  ├─ condition_id 未知 → Gamma /markets?condition_ids=
│  │  └─ 返回: clobTokenIds[], question, outcomes
│  └─ market_id 未知 → Gamma /markets?question_ids= + /events?slug=
│     └─ 步骤: market_id+"00" → question_id → slug → all markets
└─ 输出
   ├─ history_root[user][block_key].push(event) 增量
   ├─ user_states[user].positions 实时更新
   └─ tokens[]/conditions[] 映射可能被补齐
```


--- snapshot.json ---
存储用户在特定区块的详细持仓情况 (历史快照)
说明: checkpoint 是 per-user, 不保证不同用户的 block_number 相同

{
  "0xuser1": {
    "12345678": {                    // block_num as key
      "block_number": 12345678,
      "captured_at_unix_sec": 1705312496,
      "stable_balances": {
        "usdc_raw": "0",
        "usdc_e_raw": "1000000000",
        "usdt_raw": "0",
        "wrapped_raw": "500000000"
      },
      "positions": [
        {"token_id": "123...", "amount_raw": "1000000"}
      ]
    }
  }
}

--- history.json ---
存储统一格式的历史交易记录
来源可能是 websocket 实时日志,也可能是启动/重连时通过 RPC 补齐的 gap 日志；落地时不做来源区分
只parse 买入,卖出,铸造,合并,赎回,转换, 忽略池操作/转账
只落地统一格式的用户仓位事件
TransferSingle/TransferBatch 不作为独立事件类型暴露,但会参与 order / split / merge / redeem / convert 的语义展开与闭环校验
忽略池操作和不影响用户仓位的非目标腿

事件订阅表 (按用户地址过滤):
| 事件             | Contract                                          | Topic                                                              | 用户字段位置 |
| ---------------- | ------------------------------------------------- | ------------------------------------------------------------------ | ------------ |
| OrderFilled      | 0x4bfb41d5b3570defd03c39a9a4d8de6bd8b8982e (CTF)  | 0xd0a08e8c493f9c94f29311604c9de1b4e8c8d4c06bd0c789af57f2d65bfec0f6 | topics[2,3]  |
| OrderFilled      | 0xc5d563a36ae78145c45a50134d48a1215220f80a (NR)   | 0xd0a08e8c493f9c94f29311604c9de1b4e8c8d4c06bd0c789af57f2d65bfec0f6 | topics[2,3]  |
| PositionSplit    | 0x4d97dcd97ec945f40cf65f87097ace5ea0476045 (CTF)  | 0x2e6bb91f8cbcda0c93623c54d0403a43514fabc40084ec96b6d5379a74786298 | topics[1]    |
| PositionsMerge   | 0x4d97dcd97ec945f40cf65f87097ace5ea0476045 (CTF)  | 0x6f13ca62553fcc2bcd2372180a43949c1e4cebba603901ede2f4e14f36b282ca | topics[1]    |
| PayoutRedemption | 0x4d97dcd97ec945f40cf65f87097ace5ea0476045 (CTF)  | 0x2682012a4a4f1973119f1c9b90745d1bd91fa2bab387344f044cb3586864d18d | topics[1]    |
| PositionConvert  | 0xd91e80cf2e7be2e162c6513ced06f1dd0da35296 (NRA)  | 0xb03d19dddbc72a87e735ff0ea3b57bef133ebe44e1894284916a84044deb367e | topics[1]    |

事件字段与缺失信息补齐:
| 事件             | 原始字段                                                     | 缺失        | 补齐方式                                                    |
| ---------------- | ------------------------------------------------------------ | ----------- | ----------------------------------------------------------- |
| OrderFilled      | maker, taker, token_id, token_amount, collateral_amount, fee | -           | 完整，无需补齐                                              |
| PositionSplit    | stakeholder, condition_id, partition[], amount, collateral   | token_id    | token_id = getPositionId(collateral, condition, idx)        |
| PositionsMerge   | stakeholder, condition_id, partition[], amount, collateral   | token_id    | token_id = getPositionId(collateral, condition, idx)        |
| PayoutRedemption | redeemer, condition_id, index_sets[], payout                 | token_id    | token_id = getPositionId(collateral, condition, idx)        |
| PositionConvert  | stakeholder, market_id, index_set, amount                    | 仓位腿      | 不直接写 history; 结合同 tx Transfer 展开为多个 Convert leg |

映射补齐 (存入 meta.json，缺失时点查):
| 映射                     | 来源                          | 补齐方式                                                                              |
| ------------------------ | ----------------------------- | ------------------------------------------------------------------------------------- |
| condition_id → collateral| PositionSplit/Merge 事件      | 事件自带 collateral_token 字段                                                        |
| market_id → question_ids | Gamma API                     | /markets?question_ids=market_id+"00" → slug → /events?slug= → all questionIDs         |
| question_id → condition  | 计算                          | condition = keccak256(NRA, question_id, 2)                                            |
| token_id → (cond, idx)   | Gamma API / Split/Merge 事件  | /markets?clob_token_ids= → clobTokenIds 数组位置推断 idx                              |
| (cond, idx) → token_id   | 正向计算                      | token_id = getPositionId(collateral, condition, idx)                                  |

补齐用常量:
  NegRiskAdapter     = 0xd91e80cf2e7be2e162c6513ced06f1dd0da35296

Collateral 地址 (Polygon):
  USDC (native)      = 0x3c499c542cef5e3811e1192ce70d8cc03d5c3359
  USDCe (bridged)    = 0x2791bca1f2de4661ed88a30c99a7a9449aa84174
  USDT               = 0xc2132d05d31c914a87c6611c10748aeb04b58e8f
  WrappedUSDCe       = 0x3a3bd7bb9528e159577f7c2e685cc81a765002e2  (NegRisk 专用)

Collateral 枚举:
  0=Unknown, 1=USDC, 2=USDCe, 3=USDT, 4=WrappedUSDCe

EventType 枚举:
  0=OrderBuy, 1=OrderSell, 2=Split, 3=Merge, 4=Redeem, 5=Convert

事件唯一键:
  raw_log_key = "{block_number}|{tx_hash}|{log_index}|{address}"
  op_key      = 语义根 raw log 的 raw_log_key
  event_id    = "{op_key}|{user}|{leg_index}"
  注: 同一 raw log 可能产出多条 history 事件 (双边订单 / 多 outcome / Convert 多腿)
  注: history 去重与幂等写入统一按 event_id, 不按 (block_number, log_index)

事件格式 (参考 stage2 RawEvent):
{
  "0xuser1": {
    "12345678": [                    // block_num as key, array of events
      {
        "event_id": "12345678|0xhash|5|0xd91e...|0xuser1|0",
        "op_key": "12345678|0xhash|5|0xd91e...",
        "log_index": 5,              // int64, 事件在区块内的位置
        "leg_index": 0,              // 同一 op_key 下的展开序号
        "type": 5,                   // uint8, EventType 枚举
        "condition_id": "0x...",     // bytes32, condition 标识
        "token_idx": 0,              // uint8, outcome index (0=YES, 1=NO, 255=unknown)
        "collateral": 1,             // uint8, 0=Unknown, 1=USDC, 2=USDCe, 3=USDT, 4=WrappedUSDCe
        "amount": -1000000,          // int64, 带符号 (流入+, 流出-), 1e6=$1
        "price": 0                   // int64, price*1e6, 非订单=0
      }
    ]
  }
}

--- aggregate.json ---
聚合仓位 (实时计算的当前状态)
- 每次 snapshot / gap 补齐完成后更新
- 每次 websocket 增量事件应用后触发重算
- 用于未来交易决策
- `summary.min_snapshot_block` 是所有用户 checkpoint 的保守下界
- `summary.snapshot_blocks[user]` 保留每个用户各自的 snapshot_block

{
  "summary": {
    "min_snapshot_block": 12345678,
    "snapshot_blocks": {
      "0xuser1": 12345678,
      "0xuser2": 12345690
    },
    "last_applied_block": 12345700,
    "user_count": 10,
    "position_count": 150
  },
  "users": [...],
  "aggregate": [
    {
      "token_id": "123...",
      "holder_count": 5,
      "aggregate_weight": "0.1500000000"
    }
  ]
}

--- meta.json ---
token/condition/market 元数据

设计原则:
  - condition_id 是核心主键，token 只存指向 condition 的引用
  - 信息不重复: payout/outcome_count 等只存 condition 层
  - 三个独立 map: tokens, conditions, markets

{
  "tokens": {
    "123...": {                        // token_id (uint256 as string)
      "cond": "0xabc...",              // → condition_id (必有)
      "idx": 0,                        // outcome_index: 0=YES, 1=NO
      "price": 550000,                 // 最新价格 * 1e6 (可选, 来自 gamma)
      "price_src": "gamma"             // 价格来源 (可选)
    }
  },
  "conditions": {
    "0xabc...": {                       // condition_id (bytes32)
      "qid": "0xdef...",                // question_id (可选, NegRisk 有)
      "oc": 2,                          // outcome_count
      "coll": 2,                        // collateral enum (1=USDC, 2=USDCe, 3=USDT, 4=Wrapped)
      "tids": ["123...", "456..."],     // token_ids[idx], ""=未知占位
      "resolved": false,                // 是否已结算
      "payout": [1000000, 0],           // payout_numerators[] (结算后有值)
      "payout_d": 1000000,              // payout_denominator (结算后有值)
      "q": "Who will win?",             // market_question (gamma)
      "slug": "who-will-win",           // market_slug (gamma)
      "outcomes": ["Yes", "No"]         // market_outcomes (gamma)
    }
  },
  "markets": {
    "0x111...": {                        // market_id (NegRisk 专用)
      "qids": ["0xdef...", "0xghi..."]   // question_ids[] → 可计算 condition_ids
    }
  }
}

查找路径:
  token_id       → tokens[tid].cond → conditions[cond]
  condition_id   → conditions[cond]
  market_id      → markets[mid].qids → [getNegRiskConditionId(qid) for qid]
  (cond, idx)    → conditions[cond].tids[idx]